import random
import globals

class Map:
    """Our map """
    def generate(self):
        self.map[0]
        for i in range(0,len())
    def __init__(self):
        self.map = [0]



    def slice(self,prev):
        """Move the map to the left"""
        self.map[prev+1]= self.map[prev] + (random.randrange(0,globals.jump_height) * random(-1,1))


